# Kubernetes Environment Configuration Example
# Copy this to create your Secret in Kubernetes

# REQUIRED: Strong API key for authentication
# Generate with: openssl rand -base64 32
MCP_API_KEY=your-secure-random-api-key-here

# iCalendar Feed Configuration (JSON array)
# REQUIRED: At least one calendar feed
ICAL_FEED_CONFIGS=[{"name":"Work","url":"https://calendar.google.com/calendar/ical/YOUR_CALENDAR_ID/basic.ics"},{"name":"Personal","url":"https://outlook.office365.com/owa/calendar/YOUR_CALENDAR_ID/calendar.ics"}]

# Optional: Redis Cache (Azure Redis, AWS ElastiCache, etc.)
REDIS_HOST=
REDIS_SSL_PORT=6380
REDIS_KEY=

# Server will use these from ConfigMap:
# HOST=0.0.0.0
# PORT=8080
# DEBUG=false
# TIMEZONE=UTC
# REFRESH_INTERVAL=60
